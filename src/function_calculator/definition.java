package function_calculator;
/* This class will lex the definition line and will save an array list for 
 * the parser(use old parser) to build a prototype tree, this tree will be returned to the main class
 * where it will be saved in a file*/

/*the other(original lexer), should check or make use of the saved definitions when actually evaluating*/

public class definition {

}
